// Runtime configuration - loaded at runtime, not build time
window.APP_CONFIG = {
  API_URL: 'https://student-portal-dbmc.onrender.com/api'
  // For local development, change to: 'http://localhost:5000/api'
};
